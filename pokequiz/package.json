{
  "name": "pokequiz",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": "^20.19.0 || >=22.12.0"
  },
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "android:prepare": "npm run build && cd mobile && cordova prepare",
    "android:dev": "npm run android:prepare && cd mobile && cordova run android --device --verbose",
    "android:clean": "cd mobile && cordova clean android",
    "android:log": "adb logcat Cordova:* chromium:* *:S",
    "android:release": "npm run build && cd mobile && cordova build android --release"
  },
  "dependencies": {
    "pinia": "^3.0.3",
    "vue": "^3.5.18",
    "vue-router": "^4.5.1"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^6.0.1",
    "vite": "^7.0.6",
    "vite-plugin-vue-devtools": "^8.0.0"
  }
}
